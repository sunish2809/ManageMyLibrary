import React, { ReactNode } from 'react';
import "./Right.scss"


const Right = ({ children }) => {
  return (
    <div className="appright">
      {children} 
    </div>
  );
};

export default Right;